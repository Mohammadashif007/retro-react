import SectionTitle from "../SectionTitle/SectionTitle";


const DiscussSection = () => {
    return (
        <div>
            <SectionTitle heading={"Let’s Discuss"} subHeading={"Yes, you can run unit tests and view the results directly within the app. The integrated testing features allow for a streamlined ."}></SectionTitle>


        </div>
    );
};

export default DiscussSection;